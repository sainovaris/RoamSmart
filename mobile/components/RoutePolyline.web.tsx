type LatLng = {
  latitude: number;
  longitude: number;
};

type Props = {
  coordinates: LatLng[];
  visible?: boolean;
};

export default function RoutePolylineWeb(_props: Props) {
  return null;
}
